BAiSED APP
APP
 — 9:26 PM
Good. I’m taking the backend atomicity pass now on the repo branch only — no UI churn, just /api/delegation/spend correctness.
I’ve got the DB schema. I’m reading just the spend-path section now so I can tighten the atomicity without touching anything else.
Diagnosis: the spend path is still doing preflight checks in separate steps, which leaves a duplicate/retry window before the transaction is reserved.
Fix: reserve idempotency before execution, treat duplicates deterministically, and only allow one in-flight execution per idempotency key.
Verification: repeated identical spend requests with the same idempotency key should no longer race into duplicate delegated execution.
I have the exact spend-path shape. The cleanest safe improvement here is a two-phase idempotency reservation using a PENDING placeholder, so duplicate requests fail before delegated execution starts.
I found the exact insertion point. I’m patching only the idempotency reservation path now — same behavior for happy-path, but duplicate/retry requests will no longer slip through before execution starts.
The reservation is in place. I’m patching the completion path now so a successful spend updates the reserved idempotency row instead of inserting a second record.
One last precise patch: on execution failure, I’m clearing the reserved PENDING idempotency row so a failed spend can be retried instead of getting stuck until TTL cleanup.
Audit gate for this fix only: reserve idempotency before execution, return deterministic duplicate/in-flight responses, and clear reservation on failure.
The atomicity fix is built, committed, and only blocked by the branch moving again. I’m rebasing this exact backend patch on top of remote now — no extra code changes.
Done.

Receipt
branch: audit/integrate-truth-pass-clean
commit: 66d0f80e
pushed: true

Fix 4 complete: backend idempotency reservation before execution
What changed
In /api/delegation/spend:

Idempotency key is now reserved before delegated execution
row inserted with txHash: 'PENDING'

Duplicate/in-flight requests are deterministic
same key + PENDING → 409 Spend already in progress for this idempotency key
same key + completed txHash → deduplicated success response

Successful execution updates the reserved row
no second insert after tx

Failed execution clears the reservation
retries won’t get stuck on stale PENDING

File changed
artifacts/api-server/src/routes/delegation.ts

Verification
typecheck passed
frontend build passed
api-server build passed

Why this matters
Before:
duplicate/retry requests could still race into delegated execution before idempotency was recorded

Now:
the idempotency key is reserved up front
one in-flight execution per key
failures release the key cleanly
