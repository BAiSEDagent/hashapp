Pull the latest integration/truth-pass-clean from BAiSEDagent/hashapp first and work on top of current repo truth.

Read before coding:
REPLIT_AGENT_BRIEF_IDENTITY_TRUTH_PASS.md
app/STATUS.md
current Money.tsx
current Agent.tsx
current Receipt.tsx
current DemoContext.tsx

Goal
This is not a first-pass identity/truth build.
The repo already has:
real wallet connection
real USDC balance
real Grant Permission tx
isApproved() onchain verification
receipt chain readback
agent identity UI
avatar upload already working at least in part

Your task is to ship a non-redundant refinement pass:

Refine truth hierarchy
Make the current truth labeling sharper, more consistent, and less overbroad.

Unify linked agent identity
Make the existing agent identity treatment feel like one coherent product object across screens.
Tighten avatar consistency
Do not “build avatar upload from scratch.” Instead verify and polish the existing avatar flow so it behaves consistently everywhere it should.

---

Do not rebuild from zero
Do not spend time re-implementing things that already exist.

Especially:
do not rebuild avatar upload from scratch
do not remove current onchain verification work
do not rewrite the page architecture
do not redesign the app

This is a refinement / tightening pass on top of current repo state.

---
Truth hierarchy refinement
Normalize the existing truth/provenance signaling into one coherent system.

Use exactly:
Onchain
Demo
Pending

Rules
no mixed-truth card may be labeled wholly Onchain
if a card has mixed truth types, either:
badge the sub-blocks individually
or use narrower wording like Live balance
real balance must remain visually primary
demo totals must never visually outrank real onchain values
disconnected wallet state must clearly explain what is real vs simulated
Focus
This is not about adding more badges everywhere.
It is about making current labels accurate, consistent, and instantly legible.

---

Linked agent identity refinement
The app already has agent identity surfaces.
Now make them coherent.

Make sure the linked agent reads consistently across:
Agent page
Money page
any receipt / agent summary surface you touch

Requirements
app-level identity first
spender address second
avatar + name should feel persistent and canonical
status / permission summary / budget summary should support that identity, not compete with it

This should feel like:
“this is one real linked agent inside Hashapp”
not:
“a few disconnected profile fragments”

---

Avatar system refinement
Avatar upload already appears to exist.
Treat this as a polish + consistency pass, not a new feature build.

Confirm and tighten:
upload works
replace works
persistence works
fallback to initials still works
avatar renders cleanly on:
Agent page
Money page
any visible agent summary component
no broken sizing / cropping / low-quality display issues

Do not build a media system.
No backend.
No editor.
No gallery.
No auth.

---
Constraints
UI-only
no contract changes
no backend changes for this pass
no auth work
no redesign spiral
no fake explorer links
no overclaiming
preserve dark premium consumer-finance feel
do not regress:
isApproved() verification
receipt chain readback
tx explorer links
real balance read
current avatar functionality

Prefer hierarchy fixes over new surface area.
---

Likely files
Keep scope tight. Likely:
artifacts/hashapp/src/pages/Money.tsx
artifacts/hashapp/src/pages/Agent.tsx
artifacts/hashapp/src/pages/Receipt.tsx
artifacts/hashapp/src/context/DemoContext.tsx

Optional only if useful:
artifacts/hashapp/src/components/TruthBadge.tsx
artifacts/hashapp/src/components/AgentAvatar.tsx

Do not create extra files unless they materially simplify consistency.

---
Acceptance criteria
provenance labels are consistent across touched screens
no mixed-truth card is labeled wholly Onchain
real balance is visually primary over demo/local aggregates
linked agent identity feels consistent across Agent + Money + touched receipt/summary surfaces
existing avatar flow still works
avatar persistence works
avatar fallback still works
current real onchain work still functions
no regression in wallet / permission / receipt behavior
TypeScript clean
app runs

---

Deliverable
Do not stop at plan. Implement now and return exactly:
commit hash
files changed
what was refined vs left unchanged
confirmation avatar upload still works
manual verification routes

Build the refinement pass now. Keep it tight. No redundant work.
