I’m doing the cleanup audit now: what landed in the main repo, what belongs to fallback truth, and what should live only in the MetaMask pivot branch.
Done. Here’s the cleanup audit.

Current main working branch
branch: integration/truth-pass-clean

What is in it now
Keep — this is not clutter
These are legitimate and should stay in the fallback truth branch:

Core product / truth work
app/artifacts/hashapp/src/pages/Activity.tsx
app/artifacts/hashapp/src/pages/Money.tsx
app/artifacts/hashapp/src/pages/Agent.tsx
app/artifacts/hashapp/src/pages/Receipt.tsx
app/artifacts/hashapp/src/context/DemoContext.tsx
app/artifacts/hashapp/src/config/spendPermission.ts
app/artifacts/hashapp/src/components/AgentAvatar.tsx
app/artifacts/hashapp/src/components/TruthBadge.tsx

These are the actual Hashapp shell / truth-pass / identity / receipt improvements.

Proof assets
proof/README.md
proof/proof-output.json
proof/scripts/preflight-check.ts
proof/scripts/spend-permission-proof.ts
proof/package.json
proof/tsconfig.json

These are useful and belong in the repo.

Repo clarity docs
BRANCH_GUIDE.md
app/STATUS.md
AUDIT_LOG.md
DECISION_LOG.md

These are signal, not clutter.

---

What is borderline
These are the pieces I would call transitional / now strategically stale, but not necessarily “garbage”:

EOA / Coinbase-rail code
app/artifacts/api-server/src/routes/spend.ts
app/artifacts/api-server/src/routes/index.ts
app/artifacts/api-server/package.json

This is the server-side spend executor for the old rail.
It was a real attempt, but now it is fallback-only, not the forward lane.
Doc / prompt artifacts that may now be stale
REPLIT_AGENT_BRIEF_IDENTITY_TRUTH_PASS.md
some parts of partner-tracks/VENICE.md were just updated, so that one is okay
some older identity/truth-pass docs may still be useful historically, but not as the active build driver

These aren’t harmful, but they are no longer the main build lane.

---

What the real clutter problem is
The real clutter is not that integration/truth-pass-clean is ruined.

The real clutter problem is:
Replit kept building on frontend/truth-pass
MetaMask pivot work landed there
branch discipline broke again
So the repo problem is mostly branch contamination, not “all files in the clean branch are junk.”

---

Canonical branch model now
Fallback truth / current stable shell
integration/truth-pass-clean
contains:
best current UI shell
proof assets
truthful receipts / identity / money state
older Coinbase/EOA fallback rail

New execution pivot lane
integration/metamask-delegation-poc
should become the only branch for MetaMask Smart Accounts / delegation work
Raw / noisy Replit lane
frontend/truth-pass
should no longer be treated as a merge target
only a donor/source branch for surgical ports

---

My judgment
Did I let clutter into the repo?
Yes, in the strategic sense.
Because I let the old branch structure keep influencing new work.

Is the clean branch actually trash?
No.
It is still a useful fallback / product shell branch.