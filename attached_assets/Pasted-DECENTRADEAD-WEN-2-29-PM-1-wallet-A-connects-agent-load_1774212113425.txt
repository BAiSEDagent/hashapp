DECENTRADEAD [WEN],  — 2:29 PM
1.) wallet A connects → agent loads
switch to wallet B → agent must become null if no saved agent exists
switch back to wallet A → prior agent rehydrates (i found that it Did not hydrate).                                                                                                      2.) Remove Agent flow on /agent
His change to keep the user on /agent after remove is good only if
the Agent page truly renders onboarding when connectedAgent === null. (i found that tapping remove agent takes me back to connect agent tab)
Image
Image
BAiSED APP
APP
 — 2:30 PM
Good. That means:

Real bug
wallet rehydration is still broken
wallet A → save agent
wallet B → null
back to wallet A → should restore
it did not

That is a real failure in the wallet-scoped persistence logic.

Not actually a bug
Remove Agent sending you back to the connect-agent screen on the Agent tab is correct
That screen is the onboarding state we wanted after disconnect.

So:
Issue 1: fails
Issue 2: actually passes

Next fix target is now obvious:
repair wallet-scoped rehydration so switching back to a previously configured wallet restores that wallet’s saved agent.
DECENTRADEAD [WEN],  — 2:31 PM
also add to the fix that when connect wallet. we should always be sent to agent tab. purpose one for new users to add agent name. purpose two is two see current messages from agent
BAiSED APP
APP
 — 2:32 PM
Yes. Add that as a hard requirement.

Required behavior after wallet connect
Always send user to /agent.

Why:
new user path — they need to set agent name / finish onboarding
returning user path — they should land where the agent/messages live

That is the right default entrypoint.

So the next fix set is:

repair wallet-scoped rehydration
after wallet connect, always route to /agent
keep Remove Agent returning to Agent onboarding state
That’s the correct behavior model.
DECENTRADEAD [WEN],  — 2:38 PM
i synced go do the audit
BAiSED APP
APP
 — 2:39 PM
Audit result on the current branch truth:

Still failing
Wallet rehydration
Fail
I still do not trust the wallet-scoped agent rehydration path. Based on the behavior you reported, it is not restoring wallet A’s agent when switching back.

Always route to Agent after wallet connect
Fail
Current logic is still conditional. It should be unconditional:
connect wallet
go to /agent
no debate

Remove Agent → Agent onboarding
Pass
The behavior you described — landing on the connect-agent/onboarding state inside Agent — is correct.

So current audit state is:
rehydration: broken
post-connect routing to Agent: broken
remove agent lifecycle: correct

Those are the two real fixes still needed.